# Crear el contenedor y arranca
docker-compose up -d

# Conectar con el contenedor de forma interactiva en un terminal y ejecutar
bash (interprete de comandos):
docker exec -it mysql_dev bash

# Dentro del contenedor, conectar con mysql:
mysql -u root -p
antonio

SHOW DATABASES;
USE empresa3;
SHOW TABLES;
SELECT * FROM pedidos LIMIT 5;


# ojo si ya existe el volumen, dara error y no se creará la BD:
docker-compose down -v   # elimina contenedores y volúmenes
docker-compose up  -d    # vuelve a crear todo desde cero