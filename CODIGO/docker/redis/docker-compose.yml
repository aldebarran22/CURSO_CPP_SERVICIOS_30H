services:
  redis:
    image: redis:latest
	container_name: redis-cache
	ports:
	  - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes # modo persistentes, al arrancar restaura la claves creadas
    
volumes:
  redis-data:
	